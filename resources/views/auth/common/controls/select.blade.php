<div class="form-group">
    <label>{{ $label }}</label>
    <div>
        <select class="form-control" name="{{ $control_name }}" id="{{ $control_id }}">
            <option value="0">(Không chọn mặc định đây là loại cha)</option>
        </select>
    </div>
    <span class="help-block"></span>
</div>